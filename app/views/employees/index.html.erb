<h2>Именинники в этом месяце</h2>

<br>

<table class="table table-borderless">
  <!--<thead>
    <tr>
      <th scope="col">ФИО</th>
      <th scope="col">Дата рождения</th>
    </tr>
  </thead>-->
  <tbody>
    <% @results.each do |result| %>
      <tr>
        <td><h4><%= result.birthday.strftime('%d.%m') %></h4></td>
        <td><h4>-</h4></td>
        <td><h4><%= result.last_name.capitalize() if result.last_name.present? %> <%= result.first_name.capitalize() if result.first_name.present? %> <%= result.patronymic.capitalize() if result.patronymic.present? %></h4></td>
      </tr>
    <% end %>
  </tbody>
</table>
